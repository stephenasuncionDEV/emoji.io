import { SingleConfig } from '@/config/index'

const production: SingleConfig = {
	serverUrl: 'https://emoji-io-api.herokuapp.com',
    socketUrl: 'wss://emoji-io-api.herokuapp.com'
}

export default production